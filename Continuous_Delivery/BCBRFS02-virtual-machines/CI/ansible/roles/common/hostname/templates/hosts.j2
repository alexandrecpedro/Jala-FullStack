127.0.0.1   {{ nodename }} localhost {% for a in hostname_additional_loopback_names %} {{ a }}{% endfor %}
{% if enable_local_responder is defined and enable_local_responder %}
{{ ansible_default_ipv4.address }}    local-responder
{% endif %}

# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
{{ hostname_additional_entries }}
